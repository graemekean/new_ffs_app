<h1><%= @team.name %></h1>
<p><%= @team.name %>'s mascot is <%= @team.mascot %></p>
<%= image_tag(@team.image, alt: "alt text is always required", width:200) %>
<% @team.players.each do |player| %>
<%= player.name %>
 
  <br>
<% end %>